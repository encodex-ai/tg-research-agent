[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
markers =
    llm: marks tests that require LLM (deselect with '-m "not llm"')
    asyncio: marks async tests
